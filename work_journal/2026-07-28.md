Hey so I built some of the encoder, but I honestly then pawned this off to Codex(didn't seem too interesting). Started a little bit on the training piece.
I think some of the interesting work will actually come in implementing the loss and training. Then lots of debugging is likely required for actually getting training to work(what devices etc.)

Next steps: Implement training loop, write out a quick predictor(or get one from AI), then get training loop working.

Just get this up and training, by hook or crook, then we'll go from there. Don't be so strict about it. You'll learn how to train. Also, writing model layers is about as interesting as dirt, it'll become interesting once you start training and learning.
