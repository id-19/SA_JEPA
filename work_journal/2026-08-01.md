Hey so I made a first pass SIGREG implementation(wohoo)

Probably a bunch of work around fixing my implementation functionally and then non-func.(probably highly inefficient). Just test it out, get it working decently at big numbers, after that I should setup my training loop. 
Then, write out a solid predictor(I have to implement Mamba in PyTorch, can't skip this)...simple to begin with. 
After a v0 training loop is setup we can move forward with model and training improvements.
