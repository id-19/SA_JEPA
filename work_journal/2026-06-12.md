I missed writing a couple of entries. However, I got the encoder and patch layer working, we can now convert data inputs into a latent embedding no problem(ofc the problem of training is not done yet).

I should really have done it myself tho, figrued out and done the fixes myself....even if they were fairly simple(cast down + fix residual attention). Will do that next time.
Next work: Get predictor done -> set up training loop -> run and debug training loop+architecture(see gradients and loss curve, likely I screwed something up that will cause unstable training)