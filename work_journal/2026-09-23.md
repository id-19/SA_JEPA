# 2026-09-23 — (AI-written, 3+1, ≤200w)

**Morning (07:33–09:05) — theory only, no code:**

1. **Selective, grounded.** Selective = the recurrence's parameters become functions of the input. Nothing built so far is selective — arm 1 is time-invariant. Mamba keeps A fixed and input-independent; selectivity enters through Δ (Mamba §3.2: "selectivity in Δ is enough to ensure selectivity in (A,B)"). Answer-key: `x_proj` → delta/B/C, softplus.

2. **Diagonal arm theory.** Renorm-on-diagonal deletes A (d(eff)/d(A) ≈ 1e-8; eff = ±0.99·slider) — my call confirmed. Row ops give SINGULAR values, not eigenvalues: that was the tangle. Complex eigenvalues are generic (1000/1000 random renormed 16×16). A real `a` hosts only ω = 0 or π, but 24/25 of my sine channels (ω up to 1.2063 rad/sample) need other ω. Defective case exists in my own data: the DC channel's ideal predictor [[2,−1],[1,0]]; test is `rank(A − λI)`.

3. **Decided.** Bound `|a_j| ≤ 0.99` with learnable sign (‖diag(a)‖₂ = max|a_j| exactly → no transient growth). Init needs a rate *spread*, since a bank of timescales is the only raw material for synthesizing the frequencies a real `a` cannot host.

**Next:** build `my_diag_ssm.py` — pick the squash, pre-register loss band / curve shape / speed ratio (rung-1 4–8× still untimed), head-to-head vs 0.0299.
