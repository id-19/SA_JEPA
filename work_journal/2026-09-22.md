# 2026-09-22 — (AI-written, 3+1, ≤200w)

**Morning (08:13–08:49):**
1. Opener: batch-vectorization solved on paper — batch loop killed, state becomes (B, d_state), `h @ A_eff.T` derived from the `A @ h` identity (row j of A still drives state j; renorm stays on rows, no edit). Shapes story: only state + input-slice gain a batch axis; A, B, C parameters unchanged.
2. `_eff_a()` hoist justified (autograd sums over uses; one node vs 1000 identical nodes → gradients allclose + graph-memory win). Debug prints identified for removal. Edit landed in my_ssm.py but NOT yet correct — three review flags open.
3. Pre-registered: rung-1 speedup 4–8× (unmeasured); rung-2 T-loop killable via binomial expansion of (A)^t. Wave-0 f=0 decision parked: keep rows for now (his call).

**Evening (22:12–23:06):**
1. Fixed all three rung-1 flags: line 43 `@` form, return shape (B,T,d_out) via stack dim=1, assert added in-forward (abs row sums ≤ 0.99+1e-6, pre-transpose).
2. Row-norm question settled from first principles: `A@h` and `h@A.T` = ONE equation (A.T[i,j]=A[j,i] substitution); row sums bound both spellings. `h@A` (forgotten transpose) = genuinely different equation (column weights). Column-renorm variant valid but different model — parked as ablation.
3. Seed-0 toy2 run with batched rewrite: ~0.029 ≈ certified 0.0299 — same function, faster. Triage rule adopted: rank by AI-assisted-ML transfer or SA-JEPA/alignment value.

**Next:** Diagonal arm (rung 2) head-to-head vs 0.0299 — answers "was the full matrix worth it?" Then selective (input-dependent Delta = Mamba's move), then conv1d + SiLU gate block. Ladder logged in latest_plan.md.