# 2026-09-21 — Sunday

**Session (21:04–21:45 IST, ~41 min, teacher-coexplorer / Hermes):** Resumed post-crash context on my_ssm.py. Diagnosed randn-A explosion (ρ=4.0); designed full-matrix stability fix together: renorm-in-forward pinning abs row sums at 0.9 (Ishaan's own k=1 idea, made live) + exp(-exp(log_rate)) volume sliders. Ishaan caught his own signed-vs-absolute row-sum bug via BIBO, then the (16,) broadcast bug; both fixed solo. BIBO, determinism, shape, grad checks all PASS. Swapped into train_toy2.py: **0.0299** — beats wall 0.20245, beats my ZOH par 0.06198. Also: dual-timescale memory idea (LSTM/Compressive-Transformer lineage) filed in latest_plan.md; plan doc updated with SSM rung progression.

**Next session:** batch-vectorization re-teach first (Ishaan request: "re-teach the batch thing, I need to never forget"); then diagonal arm head-to-head; LibriSpeech mel frames. d_state sweep queued, not run (session cut).