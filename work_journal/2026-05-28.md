I built the remaining parts of the data pipeline, some bugs around clipping/padding clips to be the same size, but windsurf fixed that up. 
The v0 data pipeline is made! Plus I revised FTs a little and what STFTs are. Maybe I could spend some more time learning what mel spectrograms are, but as far as I can tell, if you bin together frequencies from an STFT, that's a mel spectrogram. Apply a log function and you're good to go.
I think I'm decently solid now on the data front, might have to push a bit more on the arch and training fronts. Find smart ways to do bigger training runs + re-use other pre-trained models.