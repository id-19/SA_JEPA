Got SIGREG done, and tested from the __main__ block. Pretty awesome. Defined tasks. 
Most imp. I started studying Mamba, got an idea of what it is again, opened the paper and learned a bit. Wrote some basic SSM code, read up about delta and discretisations. The SSM code is technically sufficient maybe, with some required grad flags. But it doesn't encapsulate the parallel convolution + other beautiful stuff about mamba(like derived delta from input).

Next up is just learn more about Mamba, define a mamba 1 implementation in PyTorch, and work upwards till a bare minimum good enough version. After that we can start implementing training....which shouldn't be that challenging except validaiton loss and linear probes to judge efficacy.
