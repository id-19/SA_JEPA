# Core dependencies for SA-JEPA
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0
transformers>=4.40.0

# Data processing
pandas>=2.0.0

# Optional: for future Mamba implementation
# mamba-ssm (will add later when needed)

# Development/testing
pytest>=7.0.0
